# Core — required by Programs 1, 2, 3, 5, 6
langgraph==1.2.9
langchain==1.3.14
langchain-core==1.5.0

# Model provider — required for Program 4 (create_agent with a real model)
# and the commented-out "REAL" model lines in Programs 1 and 3.
# Uncomment whichever provider(s) you use:
langchain-openai        # OpenAI / Azure OpenAI
# langchain-aws          # AWS Bedrock
# langchain-anthropic    # Anthropic direct

# Optional but recommended: tracing
langsmith

# Required to load the .env file (see Program setup cell)
python-dotenv
