# PDF handling
pypdf>=5.9.0
reportlab>=4.4.10

# Graph building + rendering
networkx>=3.6.1
pydot>=4.0.1
# Graphviz's `dot` binary must also be installed on the system (not a pip package):
#   Ubuntu/Debian: sudo apt-get install graphviz
#   macOS:         brew install graphviz
#   Windows:       https://graphviz.org/download/

# Basic RAG (TF-IDF retrieval — no API key needed for this part)
scikit-learn>=1.8.0

# Agent + LLM (only required if you want REAL OpenAI calls;
# everything falls back to an offline mock if OPENAI_API_KEY is unset)
langchain>=1.3.15
langchain-openai>=1.6.0
langchain-community>=0.4.2

# Notebook (only needed to open/run notebook.ipynb)
notebook>=7.3.2
ipykernel>=7.3.0
nbformat>=5.10.4
